all:
  children:
    master:
      hosts:
        98.85.153.99:
          ansible_user: ubuntu
          ansible_ssh_private_key_file: /home/muhammad-ahmad/.ssh/ubuntu_server_key.pem
    # workers:
    #   hosts:
    #     # Add worker node IPs here
