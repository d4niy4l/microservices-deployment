apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: doppler
spec:
  provider:
    doppler:
      config:
        project: purely
      auth:
        secretRef:
          name: doppler-token
          key: dopplerToken
          namespace: external-secrets